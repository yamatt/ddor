[project]
name = "ddor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.3.2",
    "markdown>=3.10",
    "nh3>=0.3.4",
    "pythorhead>=0.34.3",
    "structlog>=25.5.0",
]

[dependency-groups]
dev = ["bandit>=1.9.2", "black>=26.3.1", "isort>=8.0.1", "pytest>=8.3.4", "pytest-asyncio>=1.3.0"]

[project.scripts]
ddor = "ddor.__main__:main"
